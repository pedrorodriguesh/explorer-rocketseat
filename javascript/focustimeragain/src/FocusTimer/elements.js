//separação dos elementos da página.
// "controls" eu to pegando pra colocar nos eventos de clicks
// "minutes" é pra editar o conteúdo do <span> minutes <span>
// "seconds" é pra editar o conteúdo do <span> seconds <span>

export const controls = document.getElementById('controls') // aqui eu to exportando a section inteira, pra controlar os botões que o usuário vai clicar.

export const minutes = document.getElementById('minutes') // editar os minutos do timer

export const seconds = document.getElementById('seconds') // editar os segundos do timer