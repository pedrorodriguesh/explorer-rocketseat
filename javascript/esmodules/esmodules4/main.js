import funcaoPadrao, { date, greeting } from './util.js' // (funcaoPadrao) é a função que foi exportada com default então pode usar qualquer nome. 
                                                         // { date, greeting } são as variáveis do outro arquivo sendo importadas com exatamente o mesmo nome. 

funcaoPadrao('pedro')
