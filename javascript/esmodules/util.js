const message = "mensagem" // variável tá isolada nesse arquivo, não da pra usar ela em outro lugar

export default message // aqui a gente exporta a variável pra outro arquivo, o que normalmente não aconteceria por conta do escopo.