import { text, message} from './util.js' // aqui no import, precisa ser feito exatamente com o mesmo nome, então !!import {nomevariável, nomevariável} from (outro arquivo .js)
// a ordem das variáveis aqui não importa, só o nome que precisa ser o mesmo.

console.log(message, text) 


