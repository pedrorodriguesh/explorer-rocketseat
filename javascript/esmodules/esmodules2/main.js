import qualquerNome from './util.js' // aqui eu import com QUALQUER NOME que eu queira. Utilizando então "import (nomequeeuquero) from (outro arquivo .js)"

alert(qualquerNome) // aqui eu estou usando o nome que coloquei ali em cima, e ta me mostrando a variável que tá no outro arquivo JS.


// Eu só posso ter uma exportação default por arquivo.